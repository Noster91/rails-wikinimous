<%= render 'articles_navbar' %>
<div class="container">
  <div class="d-flex justify-content-left align-items-center">
    <h2 class="text-uppercase">Article: <%= @article.title %></h2>
    <%= link_to '<i class="fa fa-pencil"></i>'.html_safe, edit_article_path, :class => "btn btn-primary m-2" %>
    <%= link_to '<i class="fa fa-trash"></i>'.html_safe, article_path(@article),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, :class => "btn btn-danger m-2" %>
  </div>
  <div>
    <h3 class="mt-3">Content of the article</h3>
    <p class="mt-3"><%= @article.content %></p>
    <div class="d-flex justify-content-left align-items-center">
      <%= link_to 'Back To Glossary', articles_path, :class => "btn btn-primary m-2" %>
    </div>
  </div>
</div>
